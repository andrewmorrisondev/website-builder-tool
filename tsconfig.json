{
  "extends": "./tsconfig.base.json",
  "include": [
    "packages/*/**/*.ts",
    "packages/*/**/*.tsx",
    "components/**/*", // Make sure components are included
    ".next/types/**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
