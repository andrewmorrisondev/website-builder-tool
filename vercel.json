{
  "builds": [
    {
      "src": "packages/template/package.json",
      "use": "@vercel/node",
      "config": {
        "buildCommand": "ls -l ./Makefile && make -f ./Makefile project PKG=packages/template"
      }
    }
  ]
}
