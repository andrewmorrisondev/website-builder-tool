{
  "builds": [
    {
      "src": "packages/template/package.json",
      "use": "@vercel/node",
      "config": {
        "installCommand": "yarn install",
        "buildCommand": "make -f ./Makefile project PKG=packages/template"
      }
    }
  ]
}
