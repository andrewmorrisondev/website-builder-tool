"use client";
import { MetaTags, HeroBanner } from "@components/registry";

const About: React.FC = () => {
  return (
    <>
      <MetaTags />
      <HeroBanner title={"Title"} />
    </>
  );
};

export default About;
